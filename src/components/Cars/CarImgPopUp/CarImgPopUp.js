import "../../../scss/CarImgPopUp.scss";

const CarImgPopUp = (props) => {

    return <div className="popUp">
        <div className="popUp__container">
            {/*
            {'isCarThere' ? 'isPopUpOpened' ? "showOpenedPopUp" : "showClosedPopUp" : 'don`t show popUp'}
            */}
        </div>
    </div>
}

export default CarImgPopUp;